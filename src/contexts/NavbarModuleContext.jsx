import { createContext } from "react";

export const NavbarModuleContext = createContext({});

export const initialNavbarModuleContext = {
    // Reference for Navbar upper container
    reference: null,

    calendar: null
    // Other sections
};
